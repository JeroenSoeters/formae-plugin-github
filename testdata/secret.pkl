/*
 * Conformance Test: Create Secret
 *
 * Creates a GitHub Actions repository secret.
 * Note: Secret values are write-only and cannot be read back from the API.
 * Requires GITHUB_TEST_OWNER and GITHUB_TEST_REPO environment variables.
 */
amends "@formae/forma.pkl"
import "@formae/formae.pkl"

import "@github/github.pkl"
import "@github/actions/secret.pkl"

local testRunID = read("env:FORMAE_TEST_RUN_ID").toUpperCase()
local testOwner = read("env:GITHUB_TEST_OWNER")
local testRepo = read("env:GITHUB_TEST_REPO")
local stackName = "plugin-sdk-test-secret-\(testRunID)"

forma {
  new formae.Stack {
    label = stackName
    description = "Plugin SDK test for GitHub Actions Secret"
  }

  new formae.Target {
    label = "github-target"
    config = new github.Config {}
  }

  new secret.Secret {
    label = "plugin-sdk-test-secret"
    owner = testOwner
    repo = testRepo
    name = "FORMAE_TEST_\(testRunID)"
    value = "test-secret-value"
  }
}
